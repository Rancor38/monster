import { Link } from 'react-router-dom';

const Footer = () => {
    return (
        <header>
            <h1>
                Footer
            </h1>

        </header>
    );
};

export default Footer;